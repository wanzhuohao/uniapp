# 老人友好录入模式 · 进度档案

- 状态: 已取消独立老人模式（2026-09-08 用户决定恢复原手机排版）
- 当前阶段: 当前手机三阶段方案已完成本地实现和自动回归，待整体提交、推送与部署；以下老人七步阶段记录仅供追溯
- 最后更新: 2026-09-10
- 创建: 2026-09-07
- AI 评审模式: 默认（历史配置；方案已取消）
- AI 评审范围: 01-阶段评审、02-阶段评审、03-代码审核、04-测试用例审核（均为历史范围，不再触发新审核）
- AI 评审强度: 标准（历史配置）
- AI 评审收敛策略: 每个审核项最多 2 轮；标准强度首轮 1 个 Agent；默认 45 分钟（历史配置）
- 审核配置记录: 2026-09-07 · Skill 默认；用户未另行指定；2026-09-08 方案取消后不再推进审核项
- 阶段推进授权: 用户曾授权按推荐方案完成资源释放失败关闭修复审核项，通过后继续 04-自测、完成本地提交并收工；该授权不再用于已取消方案，也未授权 push 或外部写入
- 阶段推进授权记录: 2026-09-07 · 用户先要求“等待我在新任务中确认 01 后再进入设计”，后以“完成后收工”确认 01；随后第一次回复“继续”授权完成 02；第二次回复“继续”授权进入 03；用户再明确要求“不用 [worktree]，都提交吧，分 2 次”，授权两次本地提交并以普通分支继续，但未授权 push 或外部写入；用户随后要求继续写完当前任务再收工，但第 2 轮代码审核仍有严重项，按审核收敛规则暂停自动推进；2026-09-08 用户取消独立老人方案
- 提测单: —
- 关联档案: —

## 当前有效交付摘要

- 手机新建页为“基本信息 → 名单 → 预览”三阶段；名单页固定悬浮小字实时预览，且只展示小字，最终页再展示完整碑面。
- 电脑详情页没有小字浮窗，保留原完整碑面预览；手机、电脑均有“自动规整 / 撤销整理”和既有拖动排序。
- 名单可先单排乱序录入。规整按称谓的辈分/支系顺序归组，并按录入先后匹配夫妻；空白姓名照常占位，人数不齐、自定义称谓和空称谓均保留。
- 小字生成严格服从名单当前实际分排，不再隐式合并相邻夫妻排。自动规整会真实改变分排；撤销会同步恢复名单、小字预览和保存结果。规整后继续输入、增删或拖动时，旧撤销快照失效。
- 管理员口令错误返回 401 后立即清除并重新弹出输入框；只有接口成功才缓存。取消或网络/服务失败后，下次操作仍可再次输入，未知结果的写请求不自动重发。
- 当前只需部署同一次 HBuilderX 构建生成的完整前端 `dist/`；云函数、数据库 schema 和配置无变更。

## 2026-09-09～10：最后两项缺陷修复与文档同步

- `488d26d fix: 允许管理员口令校验失败后立即重新输入`：覆盖连续输错后重输、取消后下次重输、网络/服务失败不缓存、旧缓存异常清除及写请求不自动重发。
- `1a18fef fix: 小字预览严格跟随名单分排并支持撤销恢复`：移除 `generateSmall()` 的隐式夫妻并排合并，覆盖五排整理成三排再撤销、空白姓名位置和保存/预览同步恢复。
- 2026-09-10 重跑现行测试：SFC `5/0`、保存运行时 `7/0`、本地工作流 `11/0`、碑文回归 `16/0`、订单云函数本地模拟 `25/0`，合计 `64 passed, 0 failed`。
- 本轮仅更新文档并重跑自动化；未重新构建 H5、未执行浏览器/真机验收、未写云端订单、未推送或部署。

## 2026-09-08：恢复手机排版与通用优化回收

- 用户决定：不再新增老人入口或七步页面，恢复“基本信息 → 名单 → 预览”，名单编辑时固定显示小字排版，完整碑面仍在最后一步查看。
- 回退方式：在现有分支定向移除老人面板、会话/候选、独立日期确认、语音/OCR 及专用测试，不做整仓 reset，不覆盖既有 3D 功能或本地文档。专用代码可从 Git 历史恢复，最近旧版提交为 `c4069cd`。
- 保留并反哺普通新建/详情页：保存失败保留草稿、提交锁与超时重试反馈、订单加载快照及过期响应保护、清明/自定义日期切换记忆；保留小字专用 `WordPreview`。后续已移除预览阶段对相邻夫妻排的隐式合并，当前分排只由名单实际结构决定。
- 新增 `SmallTextPreview.vue`：手机排版页名单变化立即预览，不引入确认按钮；窄屏固定顶部，宽屏打开同一手机路由时固定右侧，可收起/展开和调整预览字号；监听可视视口变化及页面显隐。用户后续明确电脑版不需要小字浮窗，已从 `detail.vue` 移除，保留原整碑预览区。
- 窄屏补偿：仅压缩手机名单行及称谓/姓名输入控件留白，320px 下称谓文字不再被挤没；不重组原表单。
- 自动检查实际输出：SFC `5 passed, 0 failed`、保存运行时 `7 passed, 0 failed`、本地工作流 `5 passed, 0 failed`、碑文回归 `11 passed, 0 failed`、云函数本地模拟 `25 passed, 0 failed`，合计 `53/0`。已移除功能的用例不计入现行回归。
- 构建/页面证据：HBuilderX 4.76 输出 `DONE Build complete.`（退出码 0）；内置浏览器核对 390px/320px 窄屏、390×400 短视口、桌面详情、小字实时更新与夫妻同排、字号/折叠、滚动后固定位置、最终完整碑面及 3D 实际渲染。未发现页面控制台 warning/error。
- 验证边界：短视口是浏览器模拟，不等于 iOS/Android 真机键盘；图片导出点击未捕获下载事件，未计为 PNG 文件验收；未执行云端订单写入、生产部署或远程推送。`dist/` 已随本次前端构建更新，云函数未改动。
- 当前交付状态：`c4069cd`、`488d26d`、`1a18fef` 已本地提交；更大范围的老人版定向回退、通用优化、当前构建产物和流程档案仍在工作区，尚未整体提交、推送或部署。后续只维护原排版界面，不恢复已取消的老人模式阶段。

### 同日增量：名单自动规整

- 手机和电脑名单区均增加“自动规整 / 撤销整理”，复用 `useOrderForm`，不修改既有 draggable 接线，不增加页面或确认弹窗。
- 用户确认默认按顺序匹配夫妻：先按现有称谓选项的辈分/支系顺序归组，再将同类第 N 个子与第 N 个媳、女与婿相邻排列；曾/重同义归组但保留原文字，整理后仍可人工拖动。
- 空白姓名同样按顺序占位，不跳过；人数不齐不补人、不删人；自定义/空称谓保留在末排。已有多排按当前排顺序展开后整理，所有人员条目均保留。
- 整理/撤销同步草稿及预览；撤销只在尚未继续编辑名单时可用，避免旧快照覆盖新增、改名或人工拖动。重复点击无变化时保留原撤销快照，不在输入或保存时自动再排序。
- 本轮自动检查输出：本地工作流 `10 passed, 0 failed`（新增 5 项，含空白配对位置、重复规整/撤销、人工修改保护）；SFC `5/0`、保存运行时 `7/0`、碑文回归 `11/0`、云函数本地模拟 `25/0`，共 `58/0`。
- 浏览器实测：手机与电脑均输入/载入 `媳(空)、孙子孙甲、子夫甲、媳妻乙、子夫乙`，整理结果为第 1 排 `子夫甲、媳(空)、子夫乙、媳妻乙`、第 2 排 `孙子孙甲`，撤销后回到原单排顺序；电脑版浮窗 DOM 数量为 0，两页 warning/error 均为空。未执行云端写入。

## 仓库清单

| 仓库 | 分支 | 合码策略 | 阶段基线（来源 / SHA） | 仓状态 | PR |
|---|---|---|---|---|---|
| `D:\code\uniapp-unpacked\uniapp` | `feature/elder-friendly-entry` | 当前只做本地收口；push、PR 和部署需用户另行明确 | 03 开工前依赖基线 / `33e5d231cd811dde1919e8253667bc64d7776d86` | 七步历史提交至 `26db49c`；当前相关提交为 `c4069cd`、`488d26d`、`1a18fef`；工作区仍有未提交回退/通用优化 | — |

> 2026-09-07 只读检查发现既有未跟踪 `dist/` 与 `docs/通用3D碑型范本展示与打印文件/`，均属当前 3D 任务范围。本需求不修改、不暂存、不提交这些路径，也不切换当前脏工作区分支。

## 历史老人方案阶段记录（已终止）

- [x] 01-需求 ← 已完成并收口
- [x] 02-设计 ← 已完成并收口
- [x] 03-开发 ← 已完成并收口
- [x] 04-自测 ← 历史证据保留，但方案随后取消
- [ ] 05-提测 ← 不再进入
- [ ] 06-修复 ← 不再进入
- [ ] 07-上线 ← 不再进入
- [ ] 08-合并代码 ← 不再进入

## 历史阶段评审记录

| 阶段 | 评审 Agent / 最终轮次 | 评审报告 | AI 结论 | 阶段出口审核 |
|---|---|---|---|---|
| 01-需求 | `/root/elder_entry_req_v3_r1` / 跨设备归并审核项第 1 轮 | `reviews/01-requirement-ai-review.md` | 通过（阻断/严重/一般/建议均为 0） | 用户确认通过；01 完成，不进入 02 |
| 02-设计 | `/root/elder_entry_design_r2` / 第 2 轮定向复审 | `reviews/02-design-ai-review.md` | 通过（阻断/严重/一般/建议均为 0；首轮 8 项全部关闭） | 通过；停在 03 前 |
| 03-开发 | —（未启用 03-阶段评审） | — | 不适用（未启用 03-阶段评审） | 主 Agent 判断通过（2026-09-07；代码审核、TDD、全量回归与生产构建证据齐全） |
| 04-自测 | —（未启用 04-阶段评审） | — | 不适用（未启用 04-阶段评审） | 不通过（证据不足）：通过 21、部分 14、缺条件未执行 11、不适用 5；不得提测 |
| 05-提测 | —（未启用） | — | 不适用（未启用 05-阶段评审） | 待判断 |
| 06-修复 | —（未启用） | — | 不适用（未启用 06-阶段评审） | 待判断 |
| 07-上线 | —（未启用） | — | 不适用（未启用 07-阶段评审） | 待判断 |
| 08-合并代码 | —（未启用） | — | 不适用（未启用 08-阶段评审） | 待判断 |

## 历史专项评审记录

| 审核项 | 评审 Agent / 最终轮次 | 记录位置 | AI 结论 | 人工审核（仅未启用时） |
|---|---|---|---|---|
| 03-代码审核 | `/root/elder_release_failclosed_audit_r2`；04 增量 `/root/elder_contrast_review`、`/root/elder_interaction_code_review_r2` | `review_log.md`、`test_report.md` | 主体与对比度增量通过；交互增量首轮严重 1，候选覆盖修复后第 2 轮通过（阻断/严重/一般均 0） | 不适用 |
| 04-测试用例审核 | `/root/elder_entry_testcase_review_r2`；交互增量 `/root/elder_interaction_testcase_review_r2` | `test_report.md` §测试用例评审 | 原审核项通过；交互增量两轮后唯一缺失断言已补齐并自动验证，但按轮次上限不冒充第 3 轮独立复审通过 | 不适用 |

## 历史备忘（不作为当前待办）

- 当前卡点: 本地详情异步回写与保存并发/失败重试增量已审核通过；三台真机、真实 200% 页面缩放/系统字体、可控端侧中文语音环境、模板两页浏览器 UI 生命周期、合法 OCR 冻结样本/管理员/独立复算条件仍缺失，不得提测或上线。
- 临时决策: 首版采用“手工核心 + 端侧可证明才启用 + 候选先确认”；应用内语音无法证明端侧处理时关闭，本地 OCR 受发布门禁约束，云 OCR/语音不纳入首版。
- 现有工作区边界: 保留 `feature/generic-stele-models` 及既有未跟踪 3D 产物，不修改 `D:\code\3d-models\stele`。
- 下一步第一件事: 具备设备后优先补 T31～T33 三类真机核心矩阵与 T04 真实 200% 证据；另补 T24 两页浏览器 UI 生命周期和 T08～T10/T40/T45 可控端侧语音证据。
- 04 本地自测记录: 测试用例审核第 2 轮通过后执行 T01～T51。最终自动化为核心 `19/0`、页面集成 `6/0`、图片预检 `4/0`、SFC `4/0`、保存运行时 `5/0`、本地工作流 `2/0`、云函数 `25/0`、碑文回归 `11/0`，共 `76/0`；HBuilderX Vue 3 生产构建退出码 0。浏览器完成新建页 7 步、保存门、数据往返、详情入口、320px 与无障碍探针；控制台 warning/error 0。
- 04 缺陷修复记录: 浏览器发现老人模式成功按钮白字/绿色背景对比度约 `2.24:1`，低于 `4.5:1` 门槛；仅在共享面板内加深 primary/success/danger 操作色并保留禁用态。最终 primary/success 约 `7.08:1`、普通按钮约 `6.11:1`；独立增量审核首轮一般 1（禁用态被深色覆盖），修复后定向复核通过。
- 04 构建记录: 最终候选包 34 个文件，`index.html` SHA-256 `cbca04040715341ea691ccc9f23c737ee4f48a256238503f8e716c46c16d601b`；部署与回滚说明见 `deployment_guide.md`。
- 04 提交记录: `3077106 fix: 提升老人模式操作按钮对比度`；只包含 `components/stele/ElderEntryPanel.vue`，提交前暂存凭证扫描 0 命中；未 push。
- 04 保存时序增量: 新增两页实际保存函数运行时测试，最终自动回归 `74/0`，HBuilderX 4.76 生产构建退出码 0；入口哈希 `cbca04040715341ea691ccc9f23c737ee4f48a256238503f8e716c46c16d601b`。原审核项关闭客户标识旁路后仍残留超时分类严重项；用户授权的新固定边界审核项已在两轮内关闭全部 3 个严重项并通过。该增量后续随 `26db49c` 提交，未 push，详见 `review_log.md`。
- 04 本地工作流增量: 新增 `test-elder-local-workflows.mjs`，T02 运行详情页实际加载回调并验证旧请求晚到不回写、三次模式往返不改表单；T24 分别提取新建/详情真实老人接线，完成模板白名单候选的内存闭环。首轮独立复审发现 3 个测试假阳性风险，修复后第 2 轮在 manifest `3f5444a3…c9852` 上通过（阻断/严重/一般均 0）。自动回归累计 `76/0`；T24 仍缺两页浏览器 UI 证据，不上调为通过。该增量后续随 `26db49c` 提交，未 push。
- 04 交互语义增量: 已取消父母完整生卒日期的额外“按公历核对”门、取消 1～6 步逐步确认，并在名单步骤接入与最终输出同源的实时 `WordPreview`。代码首轮复审发现模板会覆盖语音/OCR 候选，RED/GREEN 后改为只替换旧模板候选；第 2 轮在 manifest `300ec58d…211e` 上通过（阻断/严重/一般均 0）。全量自动回归 `82/0`；HBuilderX 4.76 构建 34 文件，入口哈希 `1ca492301ec0acb0ba32d849ebbe830954b838ea81bf07b31d0a96841351bfa2`；最终包浏览器从第 1 步直达第 7 步，名单小字实时更新且仅末步统一保存，warning/error 0。已提交为 `26db49c feat: 完善老人友好录入与保存流程`，未 push。
- 04 定向恢复记录: 2026-09-07 用户回复“继续”，授权另开固定边界的“平台超时结构化分类”审核项；只处理 SR1-S01，完成 RED/GREEN、全量回归、生产构建和新的独立审核后再更新 04 自测结论。
- 03 提交现场: 老人友好共享面板、组合式逻辑、页面接线、类型与 `utils/stele/elder-entry/`、5 组测试、精确 SFC 编译依赖和生产 H5 `dist/` 已提交为 `48ddcf7 feat: 增加老人友好碑文录入模式`；未 push。评审、进度和自测报告作为本地流程档案保留，未入 Git。
- 03 自动验证: 核心 `13 passed`、页面集成 `6 passed`、图片预检 `4 passed`、4 个 SFC 静态编译通过、云函数回归 `25 passed`、碑文回归 `11 passed`；HBuilderX Vue 3 生产 H5 构建退出码 0。浏览器已复核七步确认、保存门、320px 单列、详情滚动和完整总确认；三台真机、200% 字体、端侧语音设备与 OCR 冻结样本尚未执行，OCR 生产继续关闭。
- 已完成本地提交: `33e5d23 chore: 补齐通用3D碑型交付产物`；`64bc92f docs: 完成老人友好录入需求与设计`。两次提交均未推送；当前分支无 upstream。
- 03 分支决策记录: 2026-09-07，用户明确不使用 worktree，并授权把当前内容分两次本地提交。第 1 批 `33e5d231cd811dde1919e8253667bc64d7776d86` 收口既有 3D 构建/档案；随后创建 `feature/elder-friendly-entry`，该 SHA 固定为 03 开工基线。后续交付前需等待父依赖进入主分支并 rebase，禁止直接向当前远端主分支发包含 3D 依赖链的 PR。
- 03 恢复记录: 2026-09-07，用户第二次回复“继续”，授权进入 03。执行 `git fetch --prune` 后确认远端主分支仍为 `master@ebff0212d7bc1540173d08f5c597bf43a2c53261`，当前分支领先 23 个提交且无上游；因依赖未合入主分支和共享检出目录不干净，按开工门暂停，未修改业务代码。
- 02 评审完成记录: 2026-09-07，第 1 轮评审发现严重 6、一般 2，主 Agent 全部判定成立并收敛设计；第 2 轮新 Agent 在 manifest `cdef7915e3e40b62f49e2451e828617b51ca5e9bf150b0434492e104b02f3f97` 上前后两次复算一致，结论通过，四级问题均为 0。详见 `reviews/02-design-ai-review.md`。
- 阶段恢复记录: 2026-09-07，用户回复“继续”，从已完成的 01 恢复并进入 02-设计；沿用默认 AI 评审范围和标准强度，不进入业务代码开发。
- 评审完成记录: 2026-09-07，跨设备归并最小审核项第 1 轮快照完全匹配，AI 结论通过；阻断 0、严重 0、一般 0、建议 0；需求正文/预研通过后保持冻结。
- 语义变更记录: 2026-09-07，用户指示“完成后收工”，授权采用此前推荐的三设备全部非空规则；上一审核项“不通过”保留为历史。
- 语义变更记录: 2026-09-07，用户选择推荐方案 A；原审核项第 2 轮“不通过”保留，当前 01 AI 结论重置为“待复审”。
