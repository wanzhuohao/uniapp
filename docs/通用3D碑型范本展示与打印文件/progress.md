# 通用3D碑型范本展示与打印文件 · 进度档案

- 状态: 进行中
- 当前阶段: 04-自测
- 创建: 2026-09-03
- AI 评审模式: 默认
- AI 评审范围: 01-阶段评审、02-阶段评审、03-代码审核、04-测试用例审核
- AI 评审强度: 标准
- AI 评审收敛策略: 每个审核项最多 2 轮；标准强度首轮 1 个 Agent；默认 45 分钟
- 审核配置记录: 2026-09-03 · Skill 默认；用户未另行指定评审配置
- 阶段推进授权: 主 Agent 判断（本地流程、既定范围修复与阶段状态推进）
- 阶段推进授权记录: 2026-09-03 · 用户明确要求后续由 Agent 自行判断；覆盖既定需求内的评审裁决、另开最小修复审核项和阶段推进，不授权接受安全风险、删除照片、提交、推送、部署、外部写入或 PR 操作
- 提测单: —
- 关联档案: —

## 仓库清单

| 仓库 | 分支 | 合码策略 | 阶段基线（来源 / SHA） | 仓状态 | PR |
|---|---|---|---|---|---|
| `uniapp-unpacked/uniapp` | `feature/generic-stele-models` | 上线后合 | 继承已部署能力分支 `feature/direct-capability-iteration` / `ee297cb55bdb854867694bbaf76976d77b542191` | 本地提交 `29e2009` 已形成，尚未 push | — |
| `D:\code\3d-models\stele`（非 Git 工作域） | 不适用 | 本地模型不合码；仅扫描通过的匿名 GLB/STL 复制到 UniApp | 原型 `stele-template-classic-v1`：132 面、0 纹理、共同几何摘要 `c0119423...cbcaa` | 参数化首版已正式生成并发布到仓内不可变 release | — |

## 阶段记录

- [x] 01-需求 — 09-03 历史通过，后因目录与项目边界变更失效
- [x] 01-需求范围变更复核 — 两轮结束，不通过；缺陷转入经用户授权的新审核项
- [x] 01-需求 GLB 元数据最小修复审核 — 两轮结束，不通过；剩余项转入 Agent 判断的新审核项
- [x] 01-需求 STL 字段最小修复审核 — 第 2 轮通过
- [x] 01-需求最终出口 — 主 Agent 判断通过（旧“照片优先”范围；已因无照片首版变更失效）
- [x] 01-需求无照片首版范围变更复核 — 第 2 轮通过；主 Agent 判断通过
- [x] 02-设计 — 两轮 AI 评审后修订，由用户人工确认末轮修订
- [x] 03-开发 — 代码审核原项两轮结束后，末轮遗留最小修复审核第 2 轮通过；主 Agent 判断出口通过
- [ ] 04-自测 ← 当前
- [ ] 05-提测
- [ ] 06-修复
- [ ] 07-上线
- [ ] 08-合并代码

## 阶段评审记录

| 阶段 | 评审 Agent / 最终轮次 | 评审报告 | AI 结论 | 阶段出口审核 |
|---|---|---|---|---|
| 01-需求 | `generic_3d_no_photo_req_r2` / 第 2 轮 | `reviews/01-requirement-no-photo-scope-ai-review.md` | 通过 | 主 Agent 判断通过 / 2026-09-03；无照片参数化首版范围、可验证验收、预研、安全与未来照片隔离均满足 |
| 02-设计 | `generic_3d_no_photo_design_r2` / 第 2 轮 | `reviews/02-design-ai-review.md` | 不通过；末轮 1 个阻断已修订，未获第 3 轮复审 | 用户人工确认修订 / 2026-09-03 |
| 03-开发 | —（未启用） | — | 不适用（未启用 03-阶段评审） | 主 Agent 判断通过 / 2026-09-03；设计点、测试先行、编译、代码审核与待测变更集均满足 |
| 04-自测 | —（未启用） | — | 不适用（未启用 04-阶段评审） | 待判断 |
| 05-提测 | —（未启用） | — | 不适用（未启用 05-阶段评审） | 待判断 |
| 06-修复 | —（未启用） | — | 不适用（未启用 06-阶段评审） | 待判断 |
| 07-上线 | —（未启用） | — | 不适用（未启用 07-阶段评审） | 待判断 |
| 08-合并代码 | —（未启用） | — | 不适用（未启用 08-阶段评审） | 待判断 |

## 专项评审记录

| 审核项 | 评审 Agent / 最终轮次 | 记录位置 | AI 结论 | 人工审核（仅未启用时） |
|---|---|---|---|---|
| 03-代码审核 | `generic_3d_code_tail_fix_r2` / 末轮遗留最小修复审核第 2 轮 | `review_log.md` | 通过；原审核项遗留经独立最小审核收敛，最终 0/0/0/0 | 不适用 |
| 04-测试用例审核 | `generic_3d_txn_case_minreview_r1` / 新最小审核第 1 轮 | `test_report.md` §测试用例评审 | 通过；原严重项关闭，最终 0 阻断/0 严重，2 个一般项转执行前待办 | 不适用 |
| 04-事务断言增量审核 | `generic_3d_test_assertion_code_review_r2` / 第 2 轮 | `review_log.md` | 通过；原 3 项一般均关闭，剩余 1 项旧恶意样本测试断言一般待办 | 不适用 |
| 04-生命周期修复最小审核 | `generic_3d_test_assertion_code_review_r2` / 第 1 轮 | `review_log.md` | 通过；0/0/0/1，建议仅约束未来 Three 升级 | 不适用 |
| 04-空闲渲染测试口径增量审核 | `generic_3d_idle_render_case_review_r1_retry` / 第 1 轮 | `test_report.md` §空闲渲染增量审核处置 | 通过；0/0/4/1，5 项均转为执行门禁；此前快照命令不一致的尝试未进入内容评审 | 不适用 |

## 备忘

- 2026-09-03：用户确认建立新需求。范围为线下使用既有照片制作通用无字碑型，新增独立范本页面展示 GLB，并下载 Bambu Studio 可识别的 STL。
- 当前卡点：尚未取得用户现有照片的本地目录和可用实物尺寸；这不改变 01 需求口径，但会阻塞 03 阶段的实际模型生成。
- 临时决策：首版至少一个范本；允许对照片未覆盖的结构合理补全并明确标注；不改现有个性化 3D 预览，不做在线建模、3MF 强制交付、G-code 或打印机直连。
- 下一步第一件事：冻结 `requirement.md` 与方案级预研材料，执行默认范围内的 01 阶段独立 AI 评审。
- 2026-09-03 01 初评：Reviewer 报告 2 严重、1 一般、1 建议；主 Agent 全部裁决“修复”。已补本地非同步处理与中间产物授权清理边界、STL 毫米参考包围盒及导入尺寸容差，并统一照片样本验证为 02 设计出口门禁；待第 2 轮定向复审。
- 2026-09-03 01 定向复审：`generic_3d_req_r2` 确认 4 项发现全部关闭，阻断/严重/一般/建议均为 0，结论通过；Reviewer 已释放。主 Agent 按默认阶段推进授权完成 01 出口判断并进入 02。
- 02 当前卡点：需要用户提供既有照片的本地目录。首项动作是在非同步本地目录内生成匿名视角分级，并做低精度预览以冻结完整摄影测量、混合补全或 2.5D 路线；没有照片路径时暂停 02 出口。
- 下一步第一件事：取得照片目录并只读检查数量、分辨率和视角覆盖，不在档案中记录原始文件名、绝对路径或 EXIF。
- 2026-09-03 范围变更：用户纠正文件夹层级，线下建模不放在 `D:\code` 外或 UniApp 目录内；已创建 `D:\code\3d-models\stele\{source-photos,workspace,exports}`。该变更新增独立工作域并改变仓库/产物边界，原 01 评审和出口判断失效；因原审核项已用满 2 轮，按 dev-flow 新开“01-需求范围变更复核”审核项，不复用旧 Reviewer。
- 当前卡点：新 01 审核项待完成；之后照片固定从 `D:\code\3d-models\stele\source-photos` 读取，目录为空时暂停 02 出口。
- 下一步第一件事：冻结修订后的 requirement/research，执行新的 01 范围变更独立初评。
- 2026-09-03 新审核项第 1 轮：Reviewer 报告 0 阻断、0 严重、3 一般、1 建议并给出“有条件通过”。主 Agent 认为“绝对路径口径冲突”会使隐私验收不可判定、“隐藏元数据泄漏”直接违反跨工作域安全红线，分别上调为严重并裁决修复；性能门禁的一般项和证据版本建议也同步修复。待新的只读 Agent 做第 2 轮定向复审。
- 2026-09-03 新审核项第 2 轮：Reviewer 复算 manifest `c455682b...9bd6` 并确认交付集合未漂移；SC-R1-G01、G03、R01 与相邻不变量均关闭，但 SC-R1-G02 未关闭。现有条款只枚举部分 glTF 字段，未覆盖完整 JSON 字符串、扩展对象及内嵌/外链图片元数据，仍可能泄漏原文件名、人物信息或本机路径；结论为 1 阻断、0 严重、0 一般、0 建议，不通过。
- 当前卡点：本审核项已用满 2 轮。按 dev-flow 停止自动评审循环，等待用户决定“另开最小修复审核项”或接受该隐私风险；未决前不得进入 02。
- 下一步第一件事：如用户选择修复，建立新的最小修复审核项，把 GLB 门禁收口为完整 glTF JSON 字符串/扩展/图片元数据递归扫描、闭合允许值类别、未知项默认失败，再进行独立两轮内审核。
- 2026-09-03 用户决策：用户明确同意继续并另开最小修复审核项，不接受遗留隐私风险。新审核项只修订 GLB/STL 跨域发布脱敏门禁及其预研依据，不改变目录层级、建模范围、页面功能或打印交付格式。
- 最小修复不变量：目标为所有 GLB 字符串、扩展和纹理元数据可机械穷尽且未知项默认失败；相邻不变量为固定目录、不补拍、不上传/云端建模、不自动删除、不直连打印机、GLB 展示和 STL 打印格式均不变；权威源仍为 requirement 安全条款与 AC-09。
- 当前卡点：等待新审核项的独立第 1 轮 AI 初评；通过后由主 Agent完成 01 出口判断并进入 02。
- 下一步第一件事：冻结修订后的 requirement/research 和空工作域，创建全新只读 Reviewer 执行新审核项第 1 轮。
- 2026-09-03 最小修复审核第 1 轮：Reviewer 复算 manifest `18ee850c...a16` 匹配，报告 2 阻断、2 严重、1 一般、0 建议。主 Agent 裁决全部修复：扫描扩展到 JSON 键和值及整文件未归属字节；根目录三个子目录统一非 Git/非同步；02 冻结门禁事务契约，04 用合法与逐类恶意样本验证；文件名改为带首尾锚点和转义点的完整正则。待第 2 轮定向复审。
- 当前卡点：最小修复审核项尚未通过；第 2 轮只复查上述 5 项及直接影响范围。
- 下一步第一件事：冻结修订后的 requirement/research，创建另一个全新只读 Reviewer 做第 2 轮定向复审。
- 2026-09-03 最小修复审核第 2 轮：Reviewer 复算 manifest `f78e87f...63b4f` 匹配；MF-R1-B01、S01、S02、G01 已关闭，相邻不变量保持，但 MF-R1-B02 仍有一个二进制 STL 漏口：每个三角面记录末尾的 2 字节 attribute byte count 未强制为 `0x0000`，可在长度与 EOF 校验均通过时夹带数据。结论为 1 阻断、0 严重、0 一般、0 建议，不通过。
- 当前卡点：本审核项已用满 2 轮。按 dev-flow 停止自动评审循环，等待用户决定是否另开只处理 STL attribute byte count 的最小修复审核项；未决前不得进入 02。
- 下一步第一件事：如用户继续授权，新审核项只补“所有三角面的 attribute byte count 必须为 `0x0000`、报告非零计数、AC-09 增加对应失败反例”，不改变其他需求。
- 2026-09-03 后续授权：用户明确要求后面都由 Agent 自行判断。主 Agent 将其解释为可在既定需求与本地可逆范围内自动裁决 Reviewer 发现、另开必要的最小修复审核项并推进阶段；不把该授权扩大为接受安全风险、删除照片、提交/推送、部署或 PR 等外部/破坏性动作。
- 2026-09-03 STL 字段最小修复：主 Agent 依据上一审核项剩余阻断自主新开审核项，只增加二进制 STL 每三角面 attribute byte count=`0x0000`、报告非零计数和 AC-09 对应恶意反例；同步补充格式证据，不改变其他范围。
- 当前卡点：等待 STL 字段最小修复审核项独立第 1 轮评审；若无阻断/严重项，按 Agent 判断授权完成 01 出口并进入 02。
- 下一步第一件事：冻结本轮 requirement/research，创建全新只读 Reviewer。
- 2026-09-03 STL 字段审核第 1 轮：Reviewer 复算 manifest `c04fd35b...dfb85` 匹配，结论为 0 阻断、1 严重、0 一般、0 建议。唯一发现是正向回归未锁定合法全零二进制 STL，错误实现可拒绝全部二进制 STL 后用 ASCII STL 绕过正例；主 Agent 裁决修复。
- 修复处理：AC-05/AC-09 和 research 统一要求用一份逐三角面 attribute byte count=`0x0000` 的合法二进制 STL，报告、exports、UniApp 复制和页面下载 SHA-256 全链一致，再以该文件完成 Bambu Studio 导入、尺寸与切片验收。
- 当前卡点：等待本审核项第 2 轮定向复审，仅核验该严重项及直接影响。
- 下一步第一件事：冻结修订后的 requirement/research，创建另一个全新只读 Reviewer。
- 2026-09-03 STL 字段审核第 2 轮：Reviewer 复算 manifest `a8a9f375...a199b` 全字段匹配，确认原严重项关闭，阻断/严重/一般/建议均为 0，结论通过；Reviewer 已释放。
- 2026-09-03 01 出口：主 Agent 根据用户“后续由 Agent 自行判断”授权核验出口 checklist。功能/非功能/验收标准齐全且可验证，澄清记录与三维需求确认齐全，关键未知项已有方案级预研，实际照片质量已明确后置为 02 门禁，涉及工作域/仓库唯一，AI 当前审核项通过；01 判断通过并进入 02。
- 02 当前卡点：`D:\code\3d-models\stele\source-photos` 当前为空，无法进行匿名照片分级与低精度预览；该事实不妨碍先完成不依赖照片的页面、资产管线和安全扫描设计，但会阻塞 02 最终出口及建模路线定版。
- 2026-09-03 02 设计草案：已形成 `design.md`，冻结独立页面与首页入口、静态范本配置契约、GLB/STL 全批原子发布、闭合安全扫描、Three.js 生命周期和 AC-01～AC-13 测试映射；不修改现有个性化预览语义，不新增 API、云函数或数据库。
- 2026-09-03 环境只读检查：仓库已有 `three@0.183.2`、`GLTFLoader`、`GLTFExporter` 和 `STLExporter`；本机 PATH 未发现 Blender、COLMAP、Meshroom、MeshLab、Bambu Studio 或 glTF Validator，Python 3.11/3.14 未安装候选三维处理库。未执行依赖安装。
- 02 出口判断：主 Agent 判断暂不送 AI 评审。照片为空时无法可靠冻结建模路线、参考尺寸、verified/inferred 范围及 AC-13 性能阈值；此时评审只能审核半成品设计，不能满足 dev-flow 02 出口。
- 下一步第一件事：用户将既有照片放入 `D:\code\3d-models\stele\source-photos` 后，执行匿名只读分级和低精度预览；不移动、不重命名、不删除原始照片，不在档案中记录其原名、单文件绝对路径或 EXIF。
- 2026-09-03 无照片首版范围变更：用户说明当前没有照片，并明确要求先做可以完成的部分、由 Agent 自主判断。主 Agent 将首版收敛为固定参数生成的无纹理通用碑型，照片参考建模后置为未来增量；该变化影响目标、FR-01/FR-02、AC-02/06/11/12、设计路线和性能门禁，故旧 01 AI 结论及出口判断对新语义失效，回退至新的 01 范围变更复核。
- 新范围不变量：首版不读取照片、不上传、不冒充真实复刻；固定 `D:\code\3d-models\stele` 层级、GLB 展示、STL 下载、现有个性化预览隔离、严格安全扫描和整批原子发布均保持不变。
- 当前卡点：无照片不再是门禁；需完成新的 01 独立 AI 初评并由主 Agent裁决，之后修订 02 的参数化路线、参考尺寸和性能预算。
- 下一步第一件事：冻结 requirement 与两份 research，创建全新只读 Reviewer 执行“01-需求无照片首版范围变更复核”。
- 2026-09-03 无照片范围初评：`generic_3d_no_photo_req_r1` 自复算 manifest `da27e0c...e6cd` 匹配，报告 0 阻断、1 严重、3 一般、0 建议，结论不通过；Reviewer 已释放。严重项为旧照片预研仍把照片动作列为当前 02 前置；三项一般缺口是复制期故障、GLB/STL 同几何绑定和 STL 拓扑机械验收。
- 主 Agent 裁决：四项全部修复。只收口当前/未来路线并强化既有原子发布和格式一致性验收，不扩大产品范围，不需要新的外部授权。
- 当前卡点：修复 NP-R1-S01 后须由全新 Reviewer 做第 2 轮定向复审。
- 下一步第一件事：修订旧照片预研与 AC-05/AC-09，冻结新快照后启动第 2 轮。
- 2026-09-03 无照片范围复审：`generic_3d_no_photo_req_r2` 自复算修后 manifest `95c243cd...119c0` 匹配，确认 NP-R1-S01 关闭且相邻不变量无回归；阻断/严重/一般/建议均为 0，结论通过，Reviewer 已释放。
- 2026-09-03 新 01 出口：主 Agent 核验无照片首版 Spec 三要件、澄清、三维确认、预研、开放问题、仓库范围和 AI 门禁均满足，判断通过并进入 02。Bambu Studio 和真实浏览器验证明确后置，不冒充已完成。
- 当前卡点：无照片已不再阻塞；02 需把旧设计草案改为固定参数化路线，冻结参考尺寸、几何摘要、原子发布故障语义和性能预算，再执行独立设计评审。
- 下一步第一件事：生成不含照片的低精度参数化预览证据，修订 design.md 并冻结 02 评审包。
- 2026-09-03 参数化低精度预览：已在非 Git 工作域 `workspace/stele-template-classic-v1` 生成匿名参数、SVG/PNG 示意和一次性 GLB/STL 原型。原型未读取照片，实测 132 三角面、0 纹理、GLB 10,188 bytes、STL 6,684 bytes、物理包围盒 140×26×160 mm，共同 `geometrySha256=c0119423...cbcaa`；原型只作为 02 证据，不直接发布。
- 2026-09-03 02 设计定版：`design.md` 已改为无照片参数化路线，冻结共同三角面/轴单位映射、闭合 GLB 子集、STL 拓扑门禁、报告 schema、退出码、复制故障恢复、页面生命周期、全部 AC 映射和性能预算；数据/API/云函数变更均为无，合码策略仍为上线后合。
- 当前卡点：02-阶段评审已启用，需冻结设计包并由全新只读 Reviewer 初评；通过前不进入 03。
- 下一步第一件事：生成 02 manifest，启动标准强度单 Reviewer 合并覆盖安全、契约和发布专项。
- 2026-09-03 02 初评：`generic_3d_no_photo_design_r1` 自复算 manifest `30e8546a...290b` 匹配，报告 2 阻断、3 严重、1 一般、0 建议，结论不通过；Reviewer 已释放。主要问题是 policy/report 未闭合、报告与目录切换存在崩溃窗口、事务残留位于 static、模型异步乱序、同名 URL 缓存混批。
- 主 Agent 裁决：2 阻断和 3 严重全部修复；改用 static 外私有事务区、包含报告的不可变 release 目录一次改名发布、版本化 URL、闭合 JSON 契约与请求代次控制。物理手机型号属于一般项，转 04 补充真实设备验证，不阻塞 02。
- 2026-09-03 02 定向复审：`generic_3d_no_photo_design_r2` 自复算 manifest `3f1877fa...33f94` 匹配，确认 D-R1-B01 与 S01/S02/S03 已关闭，但 D-R1-B02 仍有 1 个阻断：公开集合哈希未冻结精确算法，且已有同 release 的幂等事务存在不可恢复崩溃组合；结论不通过，Reviewer 已释放。
- 末轮后修订：主 Agent 不接受带风险进入 03，已冻结报告投影与公开集合哈希算法；已有同 release 改为创建 active 前完整校验后 no-op，正常事务固定 `targetExistedBefore=false`，并穷举 PREPARING/STAGED/PUBLISHED 的恢复矩阵与故障注入。
- 当前卡点：02 单审核项已用满 2 轮，末轮后修订无法再发起第 3 轮 AI 复审；按 dev-flow 暂停在 02，不能由主 Agent 自审替代独立评审。
- 下一步第一件事：由用户人工确认 `design.md` §6.1 的集合哈希、幂等 no-op 与恢复矩阵；确认后进入 03，创建本需求分支并先写发布事务/异步加载 RED 测试。
- 2026-09-03 02 人工出口：用户明确确认末轮修订。02 按“AI 两轮报告 + 人工确认末轮修订”通过，进入 03；一般项 D-R1-G01 继续保留为 04 物理手机补充验证。
- 2026-09-03 03 开工：已刷新 `origin/master`；当前已部署能力分支相对 `origin/master` 领先 21 个提交且不落后。为保留已部署基线，从当前 HEAD 新建 `feature/generic-stele-models`，冻结阶段基线 `ee297cb55bdb854867694bbaf76976d77b542191`；需求文档为本地流程产物，不提交。
- 2026-09-03 03 依赖细化：为落实设计中的官方 glTF 二次校验，增加 `gltf-validator@2.0.0-dev.3.10` 开发依赖；仅发布工具导入，页面仍只使用既有 Three.js。该选择替代原设计“不新增 npm 依赖”的实现假设，已先同步 design/research，进入 04 前需完成独立的依赖增量复核。
- 2026-09-03 依赖增量初评：`generic_3d_dependency_review_r1` 复算 manifest `8d35cf81...f3031` 匹配，报告 0 阻断、1 严重、1 一般、0 建议。主 Agent 接受两项：依赖已改为精确版本，报告回读已绑定包版本和 Validator 实际返回版本；待全新 Reviewer 做第 2 轮定向复审。
- 2026-09-03 依赖增量复审：`generic_3d_dependency_review_r2_retry` 复算 manifest `55dae162...eb1ec` 匹配，阻断/严重为 0，结论通过；唯一一般项是 Validator 返回版本缺失时仍有包版本兜底。主 Agent 已按设计收紧为非空且与包版本精确相等，新增失败关闭用例后发布链路 22/22 通过；Reviewer 已释放。
- 2026-09-03 03 实现验证：全量 6 个 `tools/test-*.mjs` 脚本全部通过，10 个 Vue SFC 编译 0 失败；build guard 退出码 0，正式 release 重复发布为幂等 no-op，26 项扫描计数均为 0；依赖树、差异格式、现有预览隔离与凭证模式检查通过。
- 2026-09-03 03 代码审核第 1 轮：`generic_3d_code_review_r1` 复算 manifest `4706e4f1...3e95c8` 匹配，报告 1 阻断、4 严重、3 一般、1 建议，结论不通过；Reviewer 已释放。主 Agent 接受全部发现并完成存活事务锁、共边重叠、全屏提示、闭合失败报告、下载竞态、schema 实执行、面数绑定、固定生成出口与测试补强。
- 2026-09-03 审核修订验证：7 个 `tools/test-*.mjs` 全部通过；新增下载会话 3/3、发布/安全事务 32/32，真实双进程争锁、5 个崩溃点、3 个可恢复失败点和表驱动恶意 GLB/STL 样本均转绿；10 个 Vue SFC 编译 0 失败，build guard 及幂等发布通过。
- 2026-09-03 03 代码审核第 2 轮：`generic_3d_code_review_r2` 复算 manifest `84903132...7d5638` 匹配，结论不通过，剩余 1 阻断、2 严重、1 一般；原审核项已用满两轮。主 Agent 不接受风险，依据用户自主裁决授权另开只覆盖 4 个遗留点的最小修复审核项。
- 2026-09-03 末轮遗留修复：仅 `ESRCH` 证明 owner 停止，EPERM/未知均失败关闭；补共面边界包含检测；失败报告在可计算时使用真实集合哈希、不可计算时使用 null，契约校验全捕获且不透传原生路径/code；下载测试直接断言 abort。定向测试下载 3/3、发布 35/35 通过。
- 2026-09-03 末轮遗留最小修复审核第 1 轮：`generic_3d_code_tail_fix_r1` 复算 manifest `6ea79b87...aeac7` 匹配，确认进程探测、共面边界包含和下载 abort 已关闭；仍报告 0 阻断、2 严重、0 一般、0 建议。遗留为合法 JSON 但结构损坏的 policy 可在模块初始化崩溃，以及真实复制/报告写入错误会误归类。
- 2026-09-03 审核修订：正则不再由未校验 policy 在模块初始化时构造；真实复制和报告原子写入分别固定映射到阶段错误码，新增损坏 policy 子进程、输入校验后消失和报告目标冲突三个回归用例。发布专项 38/38 通过。
- 2026-09-03 最小修复审核第 2 轮：`generic_3d_code_tail_fix_r2` 复算 manifest `e55b85cf...10275` 匹配，两个严重项及三个相邻不变量全部 CLOSED；发布 38/38、下载 3/3、build guard 均通过，阻断/严重/一般/建议为 0/0/0/0，结论通过并已释放 Reviewer。
- 2026-09-03 03 出口：设计改动点已实现，无新增关键未知项；测试先行 RED/转绿、7 个测试脚本、10 个 SFC、build guard、幂等发布、依赖边界、差异格式、现有预览隔离和敏感信息检查均通过。待测变更集只含本需求，尚未 commit/push；03-阶段评审未启用。主 Agent依据阶段推进授权判断通过并进入 04。
- 2026-09-03 04 测试用例初评：`generic_3d_testcase_review_r1` 复算 manifest `3b32be84...45c71ca` 匹配，报告 0 阻断、5 严重、3 一般、0 建议并判定不通过。主 Agent 接受真实环境处置、事务子矩阵、资源释放量化、数据零访问、完整哈希链、测试配置清理、确定性 loading/全屏异常和底面检查缺口；实体手机仍按冻结设计作为补充，不升级为硬门禁。
- 2026-09-03 04 测试用例第 2 轮：`generic_3d_testcase_review_r2` 复算 manifest `ce4dc37a...59f1b33` 匹配，真实环境、资源释放、数据零访问、完整哈希、第二配置、loading/全屏和底面 7 项均 CLOSED；仍有 0 阻断、1 严重、0 一般、0 建议。S-02 的五个硬崩溃点与三个受控失败点仍缺唯一的首次返回/报告、恢复和终态口径。
- 2026-09-03 审核项收敛：原 04 测试用例审核已用满两轮。依据用户既有“后续由 Agent 自行判断”授权，主 Agent 不接受模糊事务风险，另开只处理 S-02 的“04 事务用例口径最小审核项”，不改需求、实现或其他测试范围。
- 2026-09-03 04 事务用例口径最小审核第 1 轮：`generic_3d_txn_case_minreview_r1` 复算 manifest `f3312656...8bd71b3` 匹配，确认五个硬崩溃点、三个受控失败点及相邻数据一致性口径均 CLOSED；计数 0 阻断、0 严重、2 一般、0 建议。两个一般项是执行前需把现有发布测试增强到逐点断言。主 Agent 按 dev-flow 规则将其列入待执行，不视为需接受的残余风险，04 测试用例审核判断通过。
- 当前卡点：真实浏览器用例需要本次 HBuilderX H5 构建；Bambu 导入需要 Bambu Studio；生产哈希/性能需要部署本次构建。其余本地用例可先执行。
- 下一步第一件事：把已审核用例原样写入 test_report，增强 T10-07/T10-08 自动化断言并执行本地自测。
- 2026-09-03 04 事务断言增强：`test-stele-model-publish.mjs` 已逐点精确断言 5 个硬崩溃与 3 个受控失败的 stderr、B/A、owner/journal、receipt、transactionId、state、目录终态及路径/堆栈脱敏；专项 38/38。两轮增量代码审核均 PASS，第二轮计数 0/0/1/0；唯一一般待办只影响旧 GLB 恶意样本的测试假阴性防护，不改变生产失败契约。
- 2026-09-03 HBuilder 构建：用本机 HBuilderX 4.76 的 Vue 3 编译器从插件工作目录执行正式 H5/web 构建，`DONE Build complete`；输出为 `unpackage/dist/build/web`，最终时间 18:31:31。build guard 退出 0，新 release、页面与路由均进入构建。
- 2026-09-03 真实浏览器自测：Codex 内置浏览器完成首页入口、返回、视觉、旋转、重置、全屏和 404 恢复；Chrome 152.0.7977.66、Edge 152.0.4191.53、Pixel 7 模拟均完成真实模型加载和旋转像素变化；404、Offline、Fullscreen 缺失/拒绝、WebGL 创建失败及 context lost 均显示可判定提示并可恢复。
- 2026-09-03 下载证据：Chrome 从本地本次构建真实点击下载 STL，文件保存在非 Git 工作域 `workspace/stele-template-classic-v1/browser-download-evidence`；6,684 bytes，SHA-256=`409a716fcd8be4bb2b2d179abac594829552d01870414f1ee9292159b4e9030d`，与 exports/release/build/本地 HTTP 一致。按测试设计保留至验收后，不自动删除。
- 2026-09-03 生命周期 Bug：首次精确探针发现 OrbitControls 的 `document.keydown` 捕获监听每次退出净增 1。按 Bug 流程先补页面契约 RED（6/7），再保存创建时事件根并在 dispose 前显式移除 keydown/keyup；修后页面测试 7/7、真实 5 次进出 listener/RAF/canvas 全回基线，各资源 dispose 5/5，无 runtime error。最小只读审核 PASS（0/0/0/1）。
- 2026-09-03 最终本地回归：7 个 `tools/test-*.mjs` 共 102 条断言全部退出 0；HBuilderX 正式构建完成；build guard、`git diff --check`、运行依赖边界、旧 `pages/stele/preview.vue` 零差异和 16 个变更文件凭证模式扫描通过。
- 2026-09-03 页面数据边界复核：全新隔离 Chrome 中，localStorage/sessionStorage 前中后均为空，页面期请求均为 GET，订单/上传/第三方建模端点为 0，runtime error 0；同时发现项目原有 `index.html` 远程字体请求及 HBuilder Web 基础样式的 DCloud CDN 阴影预加载，后者产生第三方 DCloud cookie。它们不是 3D 页新增业务访问，但不满足冻结用例的“仅同源 GET、cookie 零变化”；不擅自扩大到全站字体/编译器样式整改，也不放宽用例，T01/T12 保持部分通过。
- 2026-09-03 本地提交：按用户授权仅提交本需求代码、模型 release 与测试，提交为 `29e2009 feat: 增加通用3D碑型范本展示与下载`；流程文档、构建产物、非 Git 模型工作域和临时证据未纳入提交，尚未 push。
- 当前卡点：Bambu Studio 未安装，T08 的实际导入、140×26×160 mm 回查和切片尚未执行；本次构建尚未部署，生产 HTTP 哈希/缓存与 T13 九次性能尚未执行；T01 既有订单及第三方 cookie、T02 六方向固化、T11 第二配置夹具、T12 全同源/全 cookie 口径仍为部分通过。因此 04 暂不满足出口，不进入 05。
- 下一步第一件事：用户具备 Bambu Studio 时导入保留的浏览器下载 STL 并切片；或用户部署本次 `unpackage/dist/build/web` 后提供生产地址，再执行生产哈希与九次性能。两项互不替代。
- 2026-09-04 04 同范围优化：用户授权由 Agent 自主筛选并实施优化。主 Agent 将范围限制在通用 3D 碑型范本页，选定三项低风险增量：空闲/后台按需渲染、锁定 Three.js 精确版本以保护 OrbitControls 生命周期兼容、修复 Windows 路径转义下测试脱敏假阴性并补状态可访问性；GLB/STL、路由、订单边界和下载格式均不变，不处理全站远程字体，不部署、不提交、不 push。
- 影响传播：按需 RAF 与后台暂停改变 T07 的测试语义，原 T07 结果失效并重置为待重测；04 仍为当前阶段，其他已通过用例不因控制面更新失效。先另开“04 空闲渲染测试口径增量审核项”，通过后才写 RED 测试和实现；实现完成后执行增量 `03-代码审核` 与完整本地回归。
- 当前卡点：等待新的 T07 测试用例口径独立审核；生产、Bambu 和既有外部门禁保持不变。
- 下一步第一件事：冻结 requirement、design 的 T07 与 review_log 直接遗留证据，创建全新只读 Reviewer。
- 2026-09-04 04 空闲渲染用例审核：有效 Reviewer `generic_3d_idle_render_case_review_r1_retry` 自复算 manifest `9904bc43...d560c7` 匹配，报告 0 阻断、0 严重、4 一般、1 建议；主 Agent按固定分级规则判断通过，并把有效监听集合、RAF/render 双计数、活动态 visibility/退出组合、机械收敛时限和预热后堆基线全部纳入执行门禁。此前 `generic_3d_idle_render_case_review_r1` 因主 Agent 冻结命令误带不存在文件参数而在读取前停止，已如实记录为无效尝试。
- 当前卡点：开始按审核口径补 RED 自动化，再实施页面、依赖版本和测试最小修改；完成后须做增量 `03-代码审核` 和完整本地回归。
- 下一步第一件事：先让页面契约测试对按需 RAF、visibility 生命周期、精确 Three.js 版本和可访问状态提示失败，再实施对应代码。
- 2026-09-04 优化实现：模型页改为按需单 RAF，阻尼/自动旋转需要时续帧，静止后归零；document hidden 与 UniApp 缓存页 onHide 均停帧，onShow/visible 恢复；隐藏缓存页通过 `pageActive` 阻止 resize 等事件偷跑渲染，并断开 OrbitControls。
- 2026-09-04 兼容与测试补强：Three 从范围版本锁为精确 `0.183.2`；发布测试新增 Windows 路径转义与 `stack/detail` 递归脱敏断言；H5 `<uni-button>` 补 `role/tabindex/Enter/Space/aria-live/pressed/disabled` 语义。GLB/STL、路由、订单边界和下载格式均未变。
- 2026-09-04 生命周期二次修复：浏览器发现 onShow 早于 DOM 重挂时 OrbitControls 可把键盘回调绑定到临时页面根；第 1 轮代码 Reviewer 又复现 Ctrl 未 keyup 隐藏时当前 document 残留 `keyup`。最终清理保存根与当前根的 keydown/keyup 并复位控制状态；Chrome 根迁移和真实 Ctrl 行为探针均归零。
- 2026-09-04 增量代码审核：第 1 轮 `generic_3d_idle_render_code_review_r1` manifest `09a20d4f...3a964c`，0/1/1/0 不通过；严重项已修复。第 2 轮 `generic_3d_idle_render_code_review_r2` manifest `324d5ec7...e3c6cc`，0/0/1/0 通过；唯一一般项是浏览器行为探针尚未固化为仓库内可重复测试，保留为后续改进，不作风险接受。
- 2026-09-04 最终本地证据：7 个测试脚本共 107 个断言全过；HBuilderX 4.76 web 构建成功；10 个 SFC、build guard、依赖树、差异格式、旧 preview 隔离和敏感信息扫描通过。Chrome 152 空闲 RAF=0、活动最多 1、hidden 零绘制；首页真实入口/返回 5 轮后 listener/RAF/canvas 均回基线，renderer/controls 各 dispose 5 次；GC 堆增长 1,182,268 bytes，小于 2 MiB 门槛。
- 当前卡点：04 本地 T07 已转为通过，但 Bambu Studio 导入/切片、部署后生产哈希与九次性能、T01/T02/T11/T12 的既有完整证据仍未闭合，因此仍停留 04，不进入 05。仓库根临时 `dist/` 由早期错误导出命令产生且未纳入评审/交付，递归删除被当前工具策略阻止，需后续人工清理。
- 下一步第一件事：用户安装 Bambu Studio 后执行 T08，或部署当前正式 web 构建后执行生产哈希、缓存与 T13 九次性能；两项互不替代。本轮代码尚未 commit/push/deploy。
