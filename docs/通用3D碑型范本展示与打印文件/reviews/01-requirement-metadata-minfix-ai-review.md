# 通用3D碑型范本展示与打印文件 · 01-需求 GLB 元数据最小修复 AI 评审报告

- 评审日期: 2026-09-03
- 初评 Agent: `generic_3d_metadata_minfix_r1`
- 复审 Agent: `generic_3d_metadata_minfix_r2`
- AI 评审模式 / 强度: 默认 / 标准
- 评审构成: 1 个通用 Agent 合并覆盖安全与权限、契约与依赖、跨工作域产物边界
- 上下文隔离方式: Codex `fork_turns="none"`
- 轻量加载方式: 原生 Agent（项目指令自动注入: 是）
- 评审档位: R3
- 评审收敛策略: 新审核项最多 2 轮；已完成第 2 轮并停止自动循环
- 评审时间预算: 默认 45 分钟
- 冻结验收矩阵: requirement 安全条款、AC-09；research 中与 GLB/glTF/PNG/STL 脱敏直接相关的事实和决定
- 当前结论: 不通过（1 阻断）
- 当前有效轮次: 第 2 轮
- 当前快照核验模式: Agent 自复算
- 第 2 轮 manifest SHA-256: `f78e87f167c2261dedabc11e4253b2b8dc32dfdfa7cf08e961f2b51f4ab63b4f`

## 新审核项授权与边界

前一“01-需求范围变更复核”已使用满 2 轮，剩余 SC-R1-G02 隐私阻断项。2026-09-03 用户明确同意继续并另开最小修复审核项；本报告是新的两轮审核项，不是原审核项第 3 轮。

- 修改范围: 仅收紧 requirement 安全条款与 AC-09，并同步直接预研依据。
- 不纳入范围: 页面设计、代码实现、模型生成、照片读取、文件复制、删除、提交、推送或部署。
- 目标不变量: GLB 的全部 JSON 字符串、扩展及纹理元数据都进入机器扫描，未知项默认失败。
- 相邻不变量: `D:\code\3d-models\stele\{source-photos,workspace,exports}` 层级不变；不补拍、不上传/云端建模、不自动删除、不直连打印机；GLB 展示与 STL 打印交付不变。
- 唯一权威源: requirement 安全条款与 AC-09；research 仅提供格式事实和设计约束来源。
- 原问题反例: 敏感信息藏在未枚举的 `mesh/animation/skin/camera/accessor/buffer/bufferView/sampler.name`、`asset.generator/copyright`、任意扩展字符串，或 PNG `eXIf/iTXt/tEXt/zTXt/iCCP` 中，旧扫描仍误判通过。
- 组合回归: GLB 默认拒绝扫描 + H5 构建/网络扫描；STL 头部/对象名检查 + Bambu Studio 导入尺寸与切片；隐私门禁不得改变目录、上传和打印机边界。

## 第 1 轮冻结快照

- 仓库 / HEAD: `D:/code/uniapp-unpacked/uniapp` / `ee297cb55bdb854867694bbaf76976d77b542191`
- base: 同 HEAD；本审核路径只有未跟踪语义材料，预期提交集合为空，两层规范 diff 均为空哈希。
- requirement SHA-256: `6be472a21126de386e148916aa852d9a994741aa0bdd8aeeda3008b44f5ecb37`
- research SHA-256: `a10ec157826d314df0af2ac9a2f4dc5a1f803f2e5fbe6bbaa8528c8adc3bc09c`
- 非 Git 工作域: `D:/code/3d-models/stele`，文件清单为空；三个职责目录存在且为空。
- manifest SHA-256: `18ee850ca6bf5f35afdaad503cdbf2ee1c00423b87e10a9b199b68e184e41a16`
- 核验: Agent 自复算全部匹配。

## 第 1 轮全量初评

- 结论: 不通过；阻断 2、严重 2、一般 1、建议 0。
- Agent 状态: 结果已捕获，并由主 Agent 调用 `interrupt_agent` 释放。

| ID | 级别 | 发现 / 证据 | Reviewer 建议 | 主 Agent 裁决 / 最小处理 | 第 2 轮关注 |
|---|---|---|---|---|---|
| MF-R1-B01 | 阻断 | requirement 只递归扫描 JSON 字符串值，对象键、已允许扩展内的动态键仍可携带原始标识 | 扫描键和值；核心与扩展 schema 闭合，未知键和声明不一致默认失败 | **修复**；安全条款增加全部键值递归、禁止自定义属性/动态键、固定扩展版本与成员路径 | 构造敏感动态键时必须失败 |
| MF-R1-B02 | 阻断 | GLB 未知 chunk/未引用字节、PNG `IEND` 后、二进制/ASCII STL 尾部可绕过元数据扫描 | 规范化并验证整文件，无归属字节默认失败 | **修复**；增加 GLB chunk/可达性/空洞/尾部，PNG CRC/顺序/EOF，STL 精确长度/完整语法门禁 | 各类尾随、空洞、不可达载荷反例必须失败 |
| MF-R1-S01 | 严重 | requirement 只要求 source-photos/workspace 非同步，与 research 的整个根目录边界冲突 | 整个根目录及 exports 纳入非 Git/非同步，未过扫描仍视为敏感 | **修复**；统一三个子目录边界，仅允许复制报告哈希绑定的通过文件 | exports 扫描前不得成为同步/发布源 |
| MF-R1-S02 | 严重 | 02/04 缺少失败退出码、不复制原子语义和恶意反例契约 | 冻结报告/退出码/原子语义，04 验证逐类失败样本 | **修复**；安全门禁事务与 AC-09 增加完整契约和组合回归 | 任一失败时非零退出且目标目录哈希不变 |
| MF-R1-G01 | 一般 | 文件名表达式未锚定且 `.` 未转义 | 使用区分大小写的完整正则并绑定同一匿名编号 | **修复**；采用 `^stele-template-([a-z0-9]+(?:-[a-z0-9]+)*)\.(glb|stl)$` | 合法/非法边界名称判定一致 |

### 主 Agent 修复不变量核对

- 目标不变量: JSON 键和值、扩展、GLB/PNG/STL 容器全部可机械穷尽，未知字段或字节默认失败。
- 相邻不变量: 工作域层级、无需补拍、本机线下建模、不自动删除、不直连打印机、GLB/STL 交付保持不变。
- 权威源: requirement 安全条款与 AC-09；research 同步格式事实与 02/04 追踪，不产生第二套宽松口径。
- 原问题反例: 动态键、未知扩展成员、不可达 bufferView、非零空洞、PNG/STL 尾部、未过扫描的 exports 自动同步、批次部分失败仍复制、伪匹配文件名。
- 组合回归: 02 白名单/报告/退出码冻结 → 04 合法样本通过；逐类恶意样本失败 → 整批不复制且目标目录哈希不变 → 构建与网络再次扫描。

## 第 2 轮定向复审

- 快照核验: base/HEAD、预期提交集合、两层 diff、两个未跟踪文件哈希和非 Git 文件清单均匹配；manifest 为 `f78e87f...63b4f`。
- 结论: 不通过；阻断 1、严重 0、一般 0、建议 0。
- Agent 状态: 结果已捕获，并由主 Agent 调用 `interrupt_agent` 释放。

| 原发现 | 复审结果 | 证据 / 原因 |
|---|---|---|
| MF-R1-B01 | 已关闭 | requirement 已覆盖 JSON 全部对象键与字符串值、核心/扩展闭合、动态键/自定义属性、声明/载荷一致性和默认拒绝；AC-09 有对应反例。 |
| MF-R1-B02 | **仍存在** | GLB、PNG 和 STL 尾部/结构大部分已闭合，但二进制 STL 每个 50 字节三角面记录末尾的 2 字节 attribute byte count 未要求为 `0x0000`，可在总长度、匿名头部和 EOF 均通过时夹带数据；AC-09 也缺少该反例。 |
| MF-R1-S01 | 已关闭 | 整个工作域及三个子目录均为非 Git、非自动同步；未过扫描 exports 仍敏感，复制与报告文件名/SHA-256 绑定。 |
| MF-R1-S02 | 已关闭 | 02 门禁事务契约和 04 合法/恶意样本、退出码、整批不复制、目标哈希不变均已明确。 |
| MF-R1-G01 | 已关闭 | 文件名正则已完整锚定、点号转义，并绑定同一匿名编号。 |

- 剩余最小修复: 要求二进制 STL 每个三角面的 attribute byte count 固定为 `0x0000`，通过报告记录非零计数为 0；AC-09 增加非零字段反例，并验证非零退出、整批不复制和目标目录哈希不变。
- 相邻不变量: 目录层级、不补拍、不上传/云端建模、不自动删除、不直连打印机、GLB 展示与 STL 打印格式均保持。
- 主 Agent 裁决: 剩余发现成立且仍违反整文件默认拒绝的隐私红线；审核项已达两轮上限，不在本项内继续修改或启动第 3 轮。

## 需用户决策项

- 当前审核项已用满 2 轮且仍有 1 个阻断项。用户需决定是否另开仅覆盖二进制 STL attribute byte count 的最小修复审核项；主 Agent 建议继续修复，不接受隐藏数据风险。

## 当前阶段出口审核

- 关联 AI 轮次: 第 2 轮
- 审核状态: 阻塞
- 审核人 / 日期: —
- 意见: 第 2 轮后仍有 1 个 STL 隐私阻断项，停止自动循环并等待用户决策。
